# Наш автомат для обработки команд
def process_command(command: str) -> str:
    match command.split():
        # Шаблон 1: команда состоит строго из двух слов, первое — "включить"
        # Второе слово автоматически запишется в переменную device!
        case ["включить", device]:
            return f"Выполняю: включаю {device}..."

        # Шаблон 2: команда состоит строго из двух слов, первое — "выключить"
        case ["выключить", device]:
            return f"Выполняю: выключаю {device}..."

        # Шаблон 3: если пришло ровно одно слово "статус"
        case ["статус"]:
            return "Система работает нормально."
        case _:
            return "Ошибка: Неизвестная команда."

# Проверяем работу нашего автомата
print(process_command("включить свет"))  # Сработает шаблон 1
print(process_command("выключить телевизор"))  # Сработает шаблон 2
print(process_command("статус"))  # Сработает шаблон 3
print(process_command("привет робот"))  # Ничего не подошло -> сработает case _